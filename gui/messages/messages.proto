syntax = "proto3";

message system_status
{
    float depth = 1;
    float ref_depth = 2;
    bool status = 3;
    float piston_pos = 4;
    float control_volume = 5;
}

message command
{
  float target_depth = 1;
  bool enable = 2;
}